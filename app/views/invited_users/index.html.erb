<h1>Bienvenido <%= current_user.name %>, coordinador <%= current_user.college.name %></h1>
<%= render partial: 'form_embed',locals:{ u: InvitedUser.new} %>

<br>
<br>
<h2>Registrados Actualmente</h2>
<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @invited_users.each do |invited_user| %>
  <tr>
    <td><%= invited_user.id %></td>
    <td><%= invited_user.name %></td>
    <td><%= invited_user.email %></td>
    <td><%= link_to 'Editar', edit_invited_user_path(invited_user) %></td>
    <td><%= link_to 'Eliminar', invited_user, confirm: '¿Estás seguro?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />
