<br>
<% @u = User.where("user_profile_id = ? and college_id = ?",2,2) %>
Proyectos Totales UAQ : <%= @u.count %>

<% @u.each do |lider_proyecto| %>
	<% if lider_proyecto.applications.first.nil? %>
		<p> vacio </p>
	<%else %>
		<% @a = lider_proyecto.applications.first %>
		<div id="proyectos">
				<table style="width:100%;">
					<tr>
						<th>Proyecto</th>
						<th>Categoria</th>
						<th>Escuela</th>
						<th>Descripción</th>
					</tr>
				<tr ALIGN=CENTER>
				<td><%= @app.name %></td>
				<td><%= @app.markets.description%></td>	
				<td><%= @app.short_description %></td>
					</tr>
				</table>
		</div>
	<%end %>
<% end %>