<%= form_for(@invited_user) do |f| %>
  <!--<% if @invited_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invited_user.errors.count, "error") %> Error al modificar</h2>

      <ul>
      <% @invited_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>-->

  <div class="field">
    <%= f.text_field :name,:class=>'inputs normal', :placeholder=>'Nombre', :required=>"required" %>
  </div>
  <div class="field">
    <%= f.email_field :email,:class=>'inputs normal', :placeholder=>'Email', :required=>"required" %>
  </div>
  <div class="actions">
    <%= f.submit 'Actualizar', :action => "index", :class => 'button loginb' ,:disable_with => "Actualizando",:style=>"margin-left: 23px;float: left;width: 220px;" %>
  </div>


<% end %>
