$(document).ready(function(){

    // Esta clase permite indicar los JS que se cargaran al inicio $(document).ready();

    cargarAccionesInicio();

    cargarAccionesArbolProblemas();

    cargarAccionesArbolAcciones();

    cargarAccionesTablaRecursos();  

    cargarAccionesTablaComponentes();

});